# CrystallBudget 💎

**Умное управление семейным бюджетом с динамическим распределением доходов**

Современное PWA приложение для управления личными и семейными финансами с поддержкой произвольных периодов бюджетирования, динамическим распределением доходов по процентам и переносом остатков.

## ✨ Особенности

- 📱 **PWA** - работает как нативное приложение на любых устройствах
- 🔒 **Безопасность** - аутентификация через email/пароль с хэшированием Argon2
- 👨‍👩‍👧‍👦 **Семейный доступ** - домохозяйства с ролями участников
- 💰 **Умное распределение** - фиксированные суммы и проценты от фактического дохода
- 🔄 **Перенос остатков** - гибкие правила переноса неизрасходованных средств
- 🌍 **Мультиязычность** - русский и английский интерфейс
- 🎨 **Адаптивный дизайн** - оптимизировано для мобильных и десктопных устройств
- ⚡ **Высокая производительность** - Next.js 14 с оптимизациями для PWA

## 🏗️ Архитектура

- **Frontend**: Next.js 14 + TypeScript + Tailwind CSS
- **Backend**: Next.js API Routes + Prisma ORM
- **Database**: PostgreSQL
- **Authentication**: NextAuth.js
- **PWA**: next-pwa с кастомными стратегиями кэширования
- **Деплой**: Docker Compose + Caddy (автоматический HTTPS)

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Доступ к серверу CentOS 9 (DigitalOcean)
- Открытые порты 80 и 443 в firewall

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd crystall-budget
   ```

2. **Настройте окружение:**
   ```bash
   cp .env.example .env
   nano .env
   ```
   
   Замените `HOSTNAME` на ваш IP в формате sslip.io:
   ```env
   HOSTNAME=203-0-113-10.sslip.io  # замените на ваш IP
   NEXTAUTH_URL=https://203-0-113-10.sslip.io
   ```

3. **Запустите приложение:**
   ```bash
   docker compose build
   docker compose up -d
   ```

4. **Проверьте статус:**
   ```bash
   docker compose logs -f
   ```

### Доступ к приложению

После успешного запуска:
- **Web**: https://ваш-ip.sslip.io
- **Демо-аккаунт**: demo@crystall.local / demo1234
- **RabbitMQ UI**: http://ваш-ip:15672 (guest/guest)

## 📱 PWA Установка

Приложение автоматически предложит установку как PWA при первом посещении на поддерживаемых устройствах:

- **iOS Safari**: "Поделиться" → "На экран Домой"
- **Android Chrome**: "Добавить на главный экран"
- **Desktop**: Кнопка установки в адресной строке

## 🛠️ Основные функции

### Управление бюджетом
- Создание бюджетов с произвольными периодами
- Планирование доходов и их фактическое отслеживание
- Автоматический пересчёт процентных категорий при поступлении доходов

### Категории расходов
- Фиксированные суммы (например, 40,000₽ на продукты)
- Процентные (например, 10% от фактического дохода на транспорт)
- Цветовая индикация и иконки для быстрого распознавания

### Перенос остатков
- **SAME_CATEGORY**: в ту же категорию следующего периода
- **TO_RESERVE**: в общий резерв семьи  
- **NONE**: не переносить

### Домохозяйства
- Создание семейных бюджетов
- Роли участников: OWNER, MEMBER
- Общие категории и счета

## 🔧 API Endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/users` | POST | Регистрация пользователя |
| `/api/budgets` | GET, POST | Управление бюджетами |
| `/api/categories` | GET, POST | Управление категориями |
| `/api/transactions` | POST | Создание операций |
| `/api/households` | GET, POST | Управление домохозяйствами |

## 🗄️ База данных

### Ключевые модели:
- **User** - пользователи системы
- **Household** - домохозяйства
- **Budget** - бюджеты с произвольными периодами
- **Category** - категории расходов
- **Allocation** - распределение средств (фиксированное/процентное)
- **Transaction** - операции доходов/расходов
- **Account** - счета для учёта остатков

### Миграции:
```bash
# В контейнере приложения
docker compose exec app npx prisma migrate deploy
docker compose exec app npm run seed
```

## 🔐 Безопасность

- Пароли хэшируются с помощью Argon2
- JWT токены с коротким временем жизни
- HTTPS обязателен для PWA функций
- Валидация на клиенте и сервере
- Защита от CSRF через NextAuth.js

## 🌐 HTTPS через sslip.io

Для получения валидного TLS сертификата без покупки домена используется сервис sslip.io:

1. IP `203.0.113.10` → домен `203-0-113-10.sslip.io`
2. Caddy автоматически запрашивает сертификат Let's Encrypt
3. PWA функции работают корректно на всех устройствах

## 📊 Мониторинг

### Проверка состояния:
```bash
# Статус контейнеров
docker compose ps

# Логи приложения
docker compose logs app

# Проверка БД
docker compose exec db psql -U crystall -d crystall
```

### Метрики:
- Использование памяти: ~200MB (приложение) + ~300MB (PostgreSQL)
- CPU: <5% в обычном режиме
- Диск: ~500MB для образов + данные БД

## 🔄 Обновления

```bash
# Обновление кода
git pull origin main
docker compose build app
docker compose up -d app

# Миграции БД (если нужно)
docker compose exec app npx prisma migrate deploy
```

## 🐛 Устранение неполадок

### Caddy не получает сертификат:
- Проверьте доступность портов 80/443 снаружи
- Убедитесь, что домен резолвится в ваш IP
- Посмотрите логи: `docker compose logs caddy`

### Приложение не подключается к БД:
- Проверьте переменные окружения
- Дождитесь готовности PostgreSQL: `docker compose logs db`

### PWA не устанавливается:
- Убедитесь, что сайт работает по HTTPS
- Проверьте манифест: https://ваш-домен/manifest.json
- Очистите кэш браузера

## 🤝 Разработка

### Локальная разработка:
```bash
cd app
npm install
npm run dev
```

### Структура проекта:
```
app/
├── app/                 # Next.js 14 App Router
├── src/
│   ├── components/      # React компоненты
│   ├── lib/            # Утилиты и конфигурация
│   └── i18n/           # Переводы
├── prisma/             # Схема и миграции БД
└── public/             # Статические файлы и PWA манифест
```

## 📄 Лицензия

MIT License. Свободно для использования и модификации.

---

**CrystallBudget** - делает управление семейным бюджетом простым и понятным! 💎✨